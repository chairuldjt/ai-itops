export const metadata = {
  title: "Anthropic Compatible Setup",
  description: "Configure the Anthropic SDK and Claude Code to use AI Gateway.",
  order: 4,
}

# Anthropic Compatible Setup

The gateway also exposes an **Anthropic-compatible** Messages API. Use it with the Anthropic SDK, Claude Code, or any client that speaks the Anthropic wire protocol.

## Base URL

```
https://your-domain.com/anthropic/v1
```

## Authentication

The gateway accepts both header formats:

```
x-api-key: sk_live_YOUR_KEY
Authorization: Bearer sk_live_YOUR_KEY
```

## Python (Anthropic SDK)

```python
import anthropic

client = anthropic.Anthropic(
    base_url="https://your-domain.com/anthropic/v1",
    api_key="sk_live_YOUR_KEY"
)

message = client.messages.create(
    model="claude-sonnet-4-20250514",
    max_tokens=1024,
    messages=[{"role": "user", "content": "Hello!"}]
)
print(message.content[0].text)
```

## Node.js (Anthropic SDK)

```javascript
import Anthropic from "@anthropic-ai/sdk";

const client = new Anthropic({
  baseURL: "https://your-domain.com/anthropic/v1",
  apiKey: "sk_live_YOUR_KEY",
});

const message = await client.messages.create({
  model: "claude-sonnet-4-20250514",
  max_tokens: 1024,
  messages: [{ role: "user", content: "Hello!" }],
});

console.log(message.content[0].text);
```

## Supported endpoints

| Endpoint | Method | Description |
|----------|--------|-------------|
| `/anthropic/v1/messages` | POST | Messages API (streaming + non-streaming) |

## Model mapping

The gateway translates model names automatically:

| Your request | Upstream model |
|-------------|---------------|
| `claude-sonnet-4-20250514` | Configured upstream model |

Check the `/v1/models` endpoint to see which models are available through your gateway.

## Next steps

- [Claude Code Setup](/docs/claude-code-setup) — step-by-step Claude Code setup
- [Capability Handling](/docs/capability-handling) — understand image policies
